package by.bsu.huffman.util;

import by.bsu.huffman.entity.Node;

import java.util.HashMap;

public class Methods {
//    public Node[] countChars(String inputStr) {
//        char[] charArray = inputStr.toCharArray();
//        HashMap<Character, Integer> charsDictionary = new HashMap<>();
//        for (char elem: charArray ) {
//            charsDictionary.put(elem, charsDictionary.get(elem) == null ? 1 : charsDictionary.get(elem) + 1);
//        }
//    }
}
